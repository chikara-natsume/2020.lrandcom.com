# Suggested commands

- Install deps: `pnpm install`
- Dev server: `pnpm dev`
- Build: `pnpm build`
- Start (prod): `pnpm start`
- Static export: `pnpm export`

Notes:
- No explicit lint/test scripts in `package.json`.
