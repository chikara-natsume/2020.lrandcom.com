# Task completion checklist

- Run `pnpm dev` to verify local behavior if changes affect runtime.
- Run `pnpm build` (and optionally `pnpm start`) to verify production build.
- There are no configured lint/test scripts; add/run if introduced.
