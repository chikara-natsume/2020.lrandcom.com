import delay from './delay'
import loadImage from './loadImage'
import date from './date'
import { withImageParams } from './imageUrl'

export const functions = { delay, loadImage, date, withImageParams }
